/**
 * The four examples, in the order the page lists them. Their prose lives in `copy/`.
 *
 * The snippets each one shows used to live here too. They are written into
 * `site/template.html` now — rendered once, at build time, because they are the same in
 * every language and the showcase is no longer a React application that could format them.
 */
export const EXAMPLE_IDS = ['minimal', 'router', 'dockview', 'theme'] as const
